# Wireshark
Lets you peek into the data flowing through a network
## Capturing packets
### Tools
- Dumpcap - lets you capture traffic from a live network and write it into a file

`dumpcap -D` gives you a list of all interfaces

The `i` flag specifies the interface and `w` specifies the output file 



